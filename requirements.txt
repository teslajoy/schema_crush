# langchain v1 (includes langgraph)
langchain[anthropic]

# vector db
chromadb

# ml/embeddings
scikit-learn
sentence-transformers
transformers
torch

# data processing
pandas
numpy
pyyaml
pydantic

# utilities
python-dotenv
click
ipython

# mcp server
mcp

# testing
pytest

# ---------------------------------------------------------------------------
# optional: magneto matcher
#
# magneto is not published on pypi, so it cannot be declared as an install
# dependency of the pypi package. biobert_match and rule_match work without
# it; magneto_match raises a RuntimeError explaining this if it is missing.
#
# to enable magneto_match:
#   pip install "magneto @ git+https://github.com/VIDA-NYU/magneto-matcher.git@main#subdirectory=algorithms/magneto"
# ---------------------------------------------------------------------------